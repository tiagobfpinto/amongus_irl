services:
  - type: web
    name: amongus-irl
    env: python
    plan: free
    pythonVersion: 3.11.9
    buildCommand: pip install --no-cache-dir -r requirements.txt
    startCommand: gunicorn --bind 0.0.0.0:${PORT:-10000} app:app
